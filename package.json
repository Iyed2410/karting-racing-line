{
  "name": "karting-racing-line",
  "version": "0.1.0",
  "description": "Interactive karting racing line optimizer",
  "main": "script.js",
  "scripts": {
    "test": "jest --passWithNoTests",
    "lint": "eslint . --ext .js",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "prettier": "^2.0.0"
  }
}
